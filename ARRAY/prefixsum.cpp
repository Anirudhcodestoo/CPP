#include <iostream>
using namespace std;

int prefix2(int l,int r)
{
    int res=
}

int preprocess(int a[], int n)
{
    int res=0,i,j=1;
    for(i=0;i<i++)
    {
    res=res+(j*a[i]);
    
    }

}


int prefix(int l,int r)
{
    preprocess(a,n);
    int res;
    if(l==0)
    return t[r];

    res=t[r]-t[l-1];
    return res;
}

    void preprocess(int a[],int n)
    {
    int i,j,m,addition=a[0],int t[n];
    for(i=1;i<n;i++)
    {   
        addition=addition+a[i];
        t[m]=addition;
        m++;
    }
    }
int main(){
    int a[100];
   int n,l,r,i;
   cout<<"enter the sizw of array";
   cin>>n;
   cout<<"enter the leftmost element";
   cin>>l;
   cout<<"enter the leftmost element";
   cin>>r;
   for(i=0;i<n;i++)
   {
    cin>>a[i];
   }
   preprocess(a,n);
  cout<<prefix(l,r);
    
    return 0;
}





